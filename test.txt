( rotate back )



(define one 1)

(define foo (c p)
 (block
   (drop p)
    (free p)
    (walk one)))


(foo  3)


(define goEnd ()
  (if ( not ( not (can grab)))
      (block
        (walk 1)
        (goEnd)
   )
   (NOP)
  )
 )
(if (not ( blocked?)))


